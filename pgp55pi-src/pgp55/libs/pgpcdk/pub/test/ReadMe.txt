For the test program to run, use PGP 5.0 or PGP 5.5 client software
to generate a Diffie-Helman key as follows:
user id:	test@pgp.com
passphrase:	"test"

If you don't do this, the test program will run, but will get errors because
it won't be able to find this key.