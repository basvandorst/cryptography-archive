/*
ftpd: securid visualizza i messaggi diversi da access-denied. ovviamente
nessuna interazione e` possibile
*/

/*
securid: Wait for the code on your card to change, then log in with the new PIN
evidentemente cosi` e` piu` sicuro. uniformarsi.
Prompt unico per fix ``bug'': Enter PASSCODE:
*/

/* steld: eliminare "no such user in /etc/passwd" */

/* kill IP-OPTIONS */

/*
skey.init: core dump se non c'e` skeydconf e skeyd
skey.init: falso replay attack se skey.init da una macchina != server
*/


/* steld: -i, per utilizzo con inetd */

/* steld: SGI specials */

/* steld: unix password support bug */
/* steld: TERM, LINES, COLUMNS, DISPLAY, WINDOWID */

/* steld: ^] escape support */
/* stel: close, !, z ... support */
/* steld: skey built-in response generation support */

/*
timeout 10 secondi per errore nel protocollo di talk
timeout 30 secondi per skeyd (overall protocol)
*/

/*
more friendly error messages 
*/

/* securely manage skey-inits within skeyd */
