# Przyk³adowy plik konfiguracyjny dla PGP 2.6.3i
# Polskie znaki: ISO-8859/2 (ISO-Latin/2) w oryginalnej dystrybucji

# Linie puste oraz zaczynaj±ce siê znakiem "#" s± ignorowane.
# Du¿e i ma³e znaki nie s± rozró¿niane. Ka¿da opcja mo¿e by‡
# zmieniona z linii poleceñ przez dodanie (na przyk³ad) "+armor=on"
# Je¶li chcesz zmieni‡ któr±¶ z opcji, po prostu skasuj "#" z pocz±tku
# i ew. wpisz ¿±dane ustawienie.

# MyName okre¶la domy¶lnego w³a¶ciciela tajnego klucza, który bêdzie
# u¿ywany do robienia sygnatur. Je¶li jej nie ustawisz, i je¶li nie
# zdefiniujesz u¿ytkownika opcj± "-u" to PGP u¿yje do tego celu pierwszy
# klucz znaleziony w zbiorze tajnych kluczy (czyli najpó¼niej stworzony).
# MyName = "Jan Kowalski"

# Jêzyk w którym bêd± wy¶wietlane komunikaty
# Obecnie dostêpne s± nastêpuj±ce modu³y jêzykowe:
#       en=angielski (domy¶lny)
#       es=hiszpañski, fr=francuski, de=niemiecki, it=w³oski,
#       sv=szwedzki, da=duñski, no=norweski, fi=fiñski,
#       nl=holenderski, pl=polski, ru=rosyjski, el=grecki,
#       cz=czeski, ro=rumuñski, hu=wêgierski, sr=serbski,
#       br=brazylijski portugalski, lt=litewski, jp=japoñski
#
# Modu³y jêzykowe s± dostêpne:
#
#   http://www.ifi.uio.no/pgp/modules.shtml
#   ftp://ftp.ifi.uio.no/pub/pgp/lang/
#
# Jêzyki jeszcze niedostêpne:
#       ar=arabski, iw=hebrajski, tr=turecki, lv=³otewski, ko=koreañski,
#       pt=portugalski, is=islandzki, zh=chiñski, esp=esperanto
#
# Wiêkszo¶‡ w/w modu³ów nazwano zgodnie ze standardem ISO 639-1988,
# okre¶laj±cym skróty s³u¿ace reprezentacji nazw jêzyków.
#
Language = pl

# Zestaw znaków u¿ywany do wy¶wietlania i konwersji tekstów.
# Konwersja ma miejsce, je¶li opcja Textmode = On, lub je¶li
# w linii poleceñ znajdzie siê opcja "-t".
#
# Dostêpne zestawy znaków:
#       latin1, cp850, cp852, cp860, cp866, alt_codes, koi8, ascii
#
# Domy¶lne ustawienie to "NoConv", czyli brak jakiejkolwiek konwersji.
# Opcje: noconv, latin1 i koi8 s± równowa¿ne.
#
# Do¶wiadczenie wskazuje, ¿e jedyne nadaj±ce siê do polskich literek
# ustawienie to "NoConv".
#
CharSet = NoConv

# TMP jest nazw± katalogu w którym PGP tworzy tymczasowe pliki robocze.
# Je¶li istnieje, to pod uwagê brana jest zmienna systemowa TMP.
# Pod MS-DOSem najlepiej przeznaczy‡ do tego celu RAM dysk, pod Unixem
# katalog tmp na swoim koncie ("~/tmp").
# TMP = "e:\"

# Pager to program który bêdzie u¿ywany do przegl±dania tekstów opcj± -m.
# Je¶li nie jest ustawiony, lub jest ustawiony na "pgp", to zostanie
# u¿yta wbudowana przegl±darka.
# Pager = "list"        # gdy u¿ywasz 4DOSa
# Pager = "more"        # pod Unixem

# ArmorLines okre¶la maksymaln± d³ugo¶‡ pakietu opakowania ASCII,
# do którego bêd± pakowane pliki po podaniu -a. Ustawienie
# tej opcji na 0 wy³±cza dzielenie opakowania na czê¶ci.
ArmorLines = 0

# Nastêpuj±ce, umieszczone w komentarzu opcje zmieniaj± domy¶lne
# dzia³anie PGP. W³±czenia opcji dokonuje siê przez usuniêcie
# znaku "#" z pocz±tku linii. Powoduje to opisane w danej opcji
# zachowanie PGP.

# Armor = on          # Pakuj w ASCII wszystko co siê da (-a)
# TextMode = on       # Zawsze próbuj u¿ywa‡ opcji -t
# ClearSig = off      # Pakuj w ASCII nawet niezaszyfrowane teksty z sygnatur±
# KeepBinary = on     # Zachowaj po¶redni plik .pgp podczas dekodowania
# Verbose = 2         # Wyczerpuj±ce komunikaty diagnostyczne
# Verbose = 0         # Minimum komunikatów
# Compress = off      # Wy³±czenie kompresji dla celów diagnostycznych
# ShowPass = on       # Pokazuj has³o podczas wpisywania
# EncryptToSelf = on  # Szyfruj ka¿dy plik równie¿ twoim kluczem publ.
# AutoSign = off      # Po stworzeniu nie opatruj kluczy certyfikatem
# Legal_Kludge = off  # W³±czenie kompatybilno¶ci ze starszymi wersjami PGP

# BakRing jest nazw± pliku w którym przechowywana jest zapasowa kopia
# zbioru tajnych kluczy, zwykle jest to dyskietka. Twoje tajne klucze
# bêd± porównywane z kopi± zapasow± za ka¿dym wywo³aniem opcji -kc.
# BakRing = "a:\secring.pgp"

# Liczba w pe³ni zaufanych certyfikatów potrzebnych do uznania klucza
# za pewny.
Completes_Needed = 1

# Liczba marginalnie zaufanych certyfikatów potrzebnych do uznania klucza
# za pewny.
Marginals_Needed = 2

# Ilo¶‡ kolejnych po¶wiadczeñ przy certyfikowaniu jednych kluczy przez
# drugie.
Cert_Depth = 4

# TZFix to liczba godzin jak± PGP musi doda‡ do czasu systemowego w celu
# uzyskania czasu GMT (jest to potrzebne do ustandaryzowania datowników).
# Poniewa¿ MS-DOS kieruje siê czasem US Pacific i samodzielnie dokonuje
# poprawek do GMT, TZFix wynosi 0 dla Kalifornii, -1 dla Kolorado,
# -2 dla Chicago, -3 dla Nowego Jorku, -8 dla Londynu, -9 dla Amsterdamu
# i Warszawy. Je¶li masz prawid³owo ustawion± zmienn± systemow± TZ,
# to nie musisz przejmowa‡ siê opcj± TZFix.
# TZFix = 0

# ¦cie¿ki i nazwy zbiorów kluczy oraz pliku inicjuj±cego generator liczb
# losowych.
# PubRing = "a:\pubring.pgp"
# SecRing = "a:\secring.pgp"
# RandSeed = "a:\randseed.bin"

# Komentarz dodawany na pocz±tku opakowania ASCII
# Comment = "Requires PGP version 2.6 or later"
