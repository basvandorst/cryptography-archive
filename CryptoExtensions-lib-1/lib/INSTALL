Install the shared libs into the LD_LIBRARY_PATH, and the crypto-ext.zip
into the CLASSPATH, eg:

	cp *.so /usr/local/lib/
	cp *.zip /usr/local/javalib/

assuming that /usr/local/lib is in the LD_LIBRARY_PATH and that
/usr/local/javalib is in the CLASSPATH.

Mail any problems or comments to Java.Crypt.Bugs@systemics.com or
Java.Crypt.Comments@systemics.com.
