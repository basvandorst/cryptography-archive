#pragma once#include <UTables.h>class CKeyDragTask;class CDragSource{public:	virtual void	GetDragHiliteRgn(						RgnHandle			ioHiliteRgn,						const STableCell&	skipCell,						Boolean				globalize )	= 0;							virtual void	AddFlavors(								CKeyDragTask		*dragTask )	= 0;													virtual Boolean	GetKeyRef(						ItemReference		item, 						PGPKeyRef *			key )		= 0;	virtual Boolean	GetTextBlock(						ItemReference		item, 						void				**outData,						long				*len )		= 0;	virtual	Boolean	GetHFS(						ItemReference		item,						DragReference		dragRef,						FSSpec				*spec)		= 0;};