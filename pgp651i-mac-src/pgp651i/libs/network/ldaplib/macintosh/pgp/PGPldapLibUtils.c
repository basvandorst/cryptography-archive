/*____________________________________________________________________________	Copyright (C) 1997 Network Associates Inc. and affiliated companies.	All rights reserved.				$Id: PGPldapLibUtils.c,v 1.4 1999/03/10 02:52:44 heller Exp $____________________________________________________________________________*/#include <ctype.h>#include <stdlib.h>#include <string.h>#include "PGPldapLibUtils.h"#include "pgpDebug.h"#include "pgpMem.h"	intstrncasecmp(const char *str1, const char *str2, unsigned nchars){	// Taken from the MSL BeOS source	    int i;    int diff;    for (i = 0; i < nchars && *str1 && *str2; i++, str1++, str2++) {        diff = toupper(*str1) - toupper(*str2);        if (diff != 0) {            return (diff);        }    }    if (i == nchars) {        return (0);    }    if (*str1 == 0) {        return (-1);    } else {        return (1);    }}// This implementation is from "getdn.c"	char *strdup(char *str){	char	*p;	if ( (p = (char *) malloc( strlen( str ) + 1 )) == NULL )		return( NULL );	strcpy( p, str );	return( p );}